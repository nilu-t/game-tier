allGames = document.querySelector(".game_content").innerHTML

//TODO: can do stuff to each of the inner html elements by replacing it after sorting, adding links, drag and drop, etc.
console.log(allGames)

allGames = allGames.split("\n");

for(let i = 0; i < allGames.length; i++){


    // anchor.setAttribute("draggable", "true"); //for each of the anchors made, the element can be dragged.
    // body.append(anchor);
}

